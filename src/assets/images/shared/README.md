# 可复用流域叙事背景

`river-story-surface.png` 是黄河、长江、珠江节点介绍与治理闯关共用的背景图。

如需替换默认图片，只需以同名文件替换该文件；`RiverNarrativeSurface` 会自动应用新素材，页面组件和样式无需修改。

若某一页面需要临时使用不同图片，可向 `RiverNarrativeSurface` 传入导入后的 `backgroundImage` 属性，不影响其他流域页面。
